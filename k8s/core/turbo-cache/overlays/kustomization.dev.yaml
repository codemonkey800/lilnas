apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lilnas-dev

resources:
  - ../base

commonLabels:
  environment: dev

# Development-specific patches
patchesStrategicMerge:
  - |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: turbo-cache
    spec:
      replicas: 1
      template:
        spec:
          containers:
          - name: turbo-cache
            resources:
              requests:
                memory: "64Mi"
                cpu: "50m"
              limits:
                memory: "256Mi"
                cpu: "200m"

# Development-specific configuration
configMapGenerator:
  - name: turbo-cache-config
    behavior: merge
    literals:
      - LOG_LEVEL=debug

# Patch ingress for dev domain
patchesJson6902:
  - target:
      group: networking.k8s.io
      version: v1
      kind: Ingress
      name: turbo-cache
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: turbo.dev.lilnas.io
      - op: replace
        path: /spec/tls/0/hosts/0
        value: turbo.dev.lilnas.io
      - op: replace
        path: /metadata/annotations/cert-manager.io~1cluster-issuer
        value: letsencrypt-staging