DB_DATABASE_NAME=sync
DB_USERNAME=sync_user
DB_PASSWORD=changeme
DB_HOSTNAME=sync-db
DATABASE_URL=postgresql://sync_user:changeme@sync-db:5432/sync

# Postgres container variables (mapped from above for convenience)
POSTGRES_DB=sync
POSTGRES_USER=sync_user
POSTGRES_PASSWORD=changeme

# Auth.js
AUTH_SECRET=changeme
AUTH_URL=http://sync.localhost
