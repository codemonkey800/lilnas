services:
  nexus-code-work:
    image: nginx:alpine
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    entrypoint: ['/bin/sh', '/local-proxy/entrypoint.sh']
    command: ['nginx', '-g', 'daemon off;']
    networks:
      - lilnas-proxy
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      LOCAL_PORT: '5173'
      SERVICE_LABEL: 'nexus-code-work'
      UNREACHABLE_DESCRIPTION: 'The nexus-code-work dev server is unreachable. Start it and refresh.'
      NGINX_ENVSUBST_TEMPLATE_VARS: LOCAL_PORT
    volumes:
      - ./local-proxy/entrypoint.sh:/local-proxy/entrypoint.sh:ro
      - ./local-proxy/nginx.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./local-proxy/unreachable.html.template:/local-proxy/unreachable.html.template:ro
    healthcheck:
      test: ['CMD', 'wget', '-q', '-O', '/dev/null', 'http://127.0.0.1/nginx-health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - traefik.enable=true
      - traefik.docker.network=lilnas-proxy
      - traefik.http.routers.nexus-code-work.rule=Host(`nexus-code-work.lilnas.io`)
      - traefik.http.routers.nexus-code-work.entrypoints=websecure
      - traefik.http.routers.nexus-code-work.tls=true
      - traefik.http.routers.nexus-code-work.middlewares=lilnas-auth
      - traefik.http.services.nexus-code-work.loadbalancer.server.port=80

networks:
  lilnas-proxy:
    external: true
