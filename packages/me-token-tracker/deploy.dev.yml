services:
  me-token-tracker:
    extends:
      file: ../../infra/deploy.dev.base.yml
      service: dev-base

    environment:
      PACKAGE_NAME: me-token-tracker

    env_file:
      - ../../infra/.env.me-token-tracker
