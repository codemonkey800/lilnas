services:
  portal:
    build:
      context: ../..
      dockerfile: apps/portal/Dockerfile
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      - traefik.enable=true
      - traefik.http.routers.portal.rule=Host(`portal.lilnas.io`)
      - traefik.http.routers.portal.entrypoints=websecure
      - traefik.http.routers.portal.tls.certresolver=le
      - traefik.http.routers.portal.middlewares=lilnas-auth
      - traefik.http.services.portal.loadbalancer.server.port=8080
