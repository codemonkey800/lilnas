// Test stub. The real `server-only` package throws when imported outside React
// Server Components; jest.config.js maps `server-only` to this file so db
// modules can load under Jest. No runtime behavior — just an empty module.
export {}
